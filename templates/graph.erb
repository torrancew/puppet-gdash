title       '<%= @data['graph_title'] %>'
vtitle      '<%= @data['vtitle'] %>'
description '<%= @data['description'] %>'

<%- @data['fields'].each do |f| %>
field :<%= f['name'] %>,
  :scale => <%= f['scale'].to_f %>,
  :alias => '<%= f['alias'] %>',
  :data  => '<%= f['data'] %>'

<%- end %>
